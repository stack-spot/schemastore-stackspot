# yaml-language-server: $schema=./plugin-schema.json

#APP PLUGIN

stkApiVersion: stkframework.stackspot.com/v0.1
kind: app-plugin
metadata:
  name: relational-repository
  display-name: Relational Repository
  description: Relational repository for ITI applications.
  version: 1.0.0
  owner: iti/foundation-team

spec:
  requires:
    - connection-interface: rds-conn

  inputs:
    - label: Connection Pool Size
      type: int
      name: connection-pool-size